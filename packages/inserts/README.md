# @verajs/inserts

The VeraJS extension registry (<!--size:inserts.gzip-->322 B<!--/size:inserts.gzip--> gzip): `insert(name, callback, priority)` chains for
`'render'`, `'proxy-handler'`, `'set-handler'` and `'error'`. This is how renderers, autoloaders
and third-party capabilities attach — the module system's backbone.
