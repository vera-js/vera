# @verajs/core

The heart of VeraJS: `init`, `createStore`/`ref`/`shallowRef`, `render`, `useEffect`/
`useLayoutEffect`/`useSyncEffect`, reactive `Map`/`Set`, scoped light-DOM styles via `@scope`,
automatic effect cleanup on element removal, and the insert extension system. <!--size:core.gzip-->3.03 KB<!--/size:core.gzip--> gzip,
no dependencies, no base class, no build step required.

The complete API reference lives in the repo's `llms.txt` — written to be pasted into an AI
context window, and just as readable by people.
