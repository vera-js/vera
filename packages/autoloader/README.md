# @verajs/autoloader

Lazy component loading by tag name (<!--size:autoloader.gzip-->612 B<!--/size:autoloader.gzip--> gzip): the first time an undefined custom element
appears inside an `autoloader`-marked component, its module is fetched from a bounded URL and
defined. `extension: '.ts'` supports TypeScript dev servers.
