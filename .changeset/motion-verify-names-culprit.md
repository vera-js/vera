---
'@verajs/motion': patch
---

The cascade-override instrument names its culprit: `motion-css-overridden` now carries the
squashed element through the reporter seam (an optional third argument — element-less reporters
are unaffected).
